VERSION_ = $$Id: mistral.config 2 2021-03-24 11:50:45Z m221078 $$

[jobs]

  batch_command = sbatch

  # Environment variables for job information
  name_environ = SLURM_JOB_NAME
  id_environ = SLURM_JOBID

[MPI]

  command = "srun -l --kill-on-bad-exit=1 --cpu_bind=verbose,cores --distribution=block:block --propagate=STACK,CORE"
