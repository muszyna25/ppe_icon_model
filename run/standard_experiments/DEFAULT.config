VERSION_ = DEFAULT.config (exp) initial revision

MODEL_DIR = eval(os.path.dirname('$PWD'))
EXP_DIR = $MODEL_DIR/experiments/$EXP_ID
SCRIPT_DIR = $EXP_DIR/scripts
DATA_DIR = $EXP_DIR
WORK_DIR = $EXP_DIR

[namelists]

[jobs]
  [[run]]
    nodes = 4
    time_limit = 00:20:00
